---
import BTN1 from "../general/BTN1.astro";
---
<section class="formBox">
    <div class="stoetHeading">
        <h2>Støt os i kampen</h2>
        <p>Du kan støtte månedligt eller med engangbetaling. Alt støtte går til kampen for et plastikfrit samfund.</p>
    </div>

    <div class="antalGange">
        <input type="radio" id="enGang" name="stoetType" class="stoetBTN1 tertiaryBTN stoetRadio" checked>
        <label for="enGang" class="stoetBTN1 tertiaryBTN"></label>
        <p>Én gang</p>
    
        <input type="radio" id="manedligt" name="stoetType" class="stoetBTN1 tertiaryBTN stoetRadio">
        <label for="manedligt" class="stoetBTN1 tertiaryBTN"></label>
        <p>Månedligt</p>
    </div>
    

    <div class="stoetBeloeb">
        <input type="radio" id="stoet50" name="stoetBeloeb" class="stoetBTN2 tertiaryBTN stoetRadio" checked>
        <label for="stoet50" class="stoetBTN2 tertiaryBTN">50</label>
    
        <input type="radio" id="stoet100" name="stoetBeloeb" class="stoetBTN2 tertiaryBTN stoetRadio">
        <label for="stoet100" class="stoetBTN2 tertiaryBTN">100</label>
    
        <input type="radio" id="stoet150" name="stoetBeloeb" class="stoetBTN2 tertiaryBTN stoetRadio">
        <label for="stoet150" class="stoetBTN2 tertiaryBTN">150</label>
    </div>
    

    <div class="stoetCTA">
        <form class="andetBeloeb ">
            <input type="text" id="Andet beløb" placeholder="Andet beløb ..."></label>
        </form>
        
        <div class="stoetCTA2">
            <BTN1 data="Støt nu"/>
            <p>Og fortsæt til betaling</p>
        </div>

    </div>
</section>

<style>

label {
    font-family: CamptonBook;
}

.formBox {
        display: flex;
        flex-direction: column;
        gap: 2.5rem;
        padding: 0.5rem;
        width: 328px;
        height: 640px;
        position: absolute;
        margin: 2.5rem;
        margin-top: 10rem;
}
@keyframes slideUp {
  0% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-30px); 
  }
  100% {
    transform: translateY(0);
  }
}
.formBox {
  animation: slideUp 0.5s ease-in-out; 
}

.formBox.hidden {
  display: none;
}

    .stoetHeading {
        display: flex;
        flex-direction: column;
        gap: 20px;
        margin-top: 2rem;
        margin-left: 2rem;
        margin-right: 2rem;
    }

    .antalGange {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-left: 2rem;
        margin-right: 2rem;
    }

    .stoetBeloeb {
        display: flex;
        /* justify-content: space-evenly; */
        text-align: center; 
        gap: 2rem;
        margin-left: 2rem;
        margin-right: 2rem;
    }
    .stoetCTA {
        display: flex;
        flex-direction: column;
        justify-content: space-evenly;
        align-items: center;
        gap: 6rem;
    }

    .stoetCTA2 {
        display: flex;
        flex-direction: column;
        justify-content: space-evenly;
        align-items: center;
        gap: 1rem;
    }

    .stoetCTA2 p {
        font-size: 10px;
        font-style: italic;
        font-weight: 400;
    }

    .stoetBTN1 {
        border-radius: 50%;
        height: 30px;
        width: 30px;
        cursor: pointer;
        border: none;
        outline: none;
        background-color: transparent;
        border-style: solid;
        color: var(--light-grey);
    }

    .stoetRadio {
        display: none; /* Hide the actual radio buttons */
        }
    
    .stoetRadio + label {
        color: var(--light-grey);
        background-color: var(--dark-blue);
    }

    .stoetRadio:hover + label {
        color: var(--white);
        background-color: var(--dark-blue);
    }

    .stoetRadio:checked + label {
        color: var(--dark-blue);
        background-color: var(--white);
    }

    .stoetBTN2 {
        border-radius: 50%;
        height: 60px;
        width: 60px;
        cursor: pointer;
        border: none;
        outline: none;
        font-size: 20px;
        font-style: normal;
        font-weight: 700;
        background-color: transparent;
        color: var(--light-grey);
        border-style: solid;
        line-height: 55px;
}

    .andetBeloeb input {
        border-color: 2px solid var(--light-grey);
        border-style: solid;
        background-color: transparent;
        color: var(--light-grey);
        width: 260px; 
        height: 52px; 
        padding: 10px; 
        border-radius: 8px;
    }

    .andetBeloeb input:hover {
        color: var(--white);
        }

    .andetBeloeb input:focus {
    border-color: var(--primary-red);
    outline: none; 
    }

    @media screen and (min-width: 800px) {
        .formBox {
            width: 606px;
            height: 967px;
            gap: 5rem;
            margin: 5rem;
            margin-top: 15rem;
            padding: 2rem;
        }

        .stoetBTN1 {
            height: 48px;
            width: 48px;
        }
        .stoetBTN2 {
            height: 90px;
            width: 90px;
            font-size: 30px;
            line-height: 85px;
        }

        .andetBeloeb input {
            width: 488px;
            height: 79px;
            padding: 30px;
            font-size: larger;
        }

            .stoetCTA2 p {
            font-size: 15px;
            font-style: italic;
            font-weight: 400;
            padding-top: 0.5rem;
        }

        .stoetBeloeb {
            gap: 4rem;
        }

        .antalGange {
        gap: 1.5rem;    
        }
    }

</style>